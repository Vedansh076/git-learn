git is a software and github is a service to host git online
github - collaboration + Backup + open source
other hosts: gitlab , BitBucket
important commands:
git config --global user.name "Vedansh"
git config --global user.email "vedansh@gmail.com"

Setup SSH keys to connnect with github, github uses SSH to allow you to push code.
Password based code push is not allowed.
SSH- Secure Socket Shell

git remote -v if empty o/p => no remote configuration is Setup
git remote add name url //connnect a remote repository
e.g. git remote add origin <url> //here origin is the name of the remote repo //we can use anyother name
git remote rename oldname newname //rename the repo
git remote remove name

git push <remote> <branch> e.g. git push origin main
git push -u origin main //linking origin with the main st whenever i push from main it links main to origin
-u setup an upstream that allow you to run future command
git push
& it push code directly to github


